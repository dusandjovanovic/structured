export const styles = theme => ({
	appBar: {
		backgroundColor: theme.palette.background.paper,
		height: "54px",
		top: "64px",
		left: 0,
		padding: theme.spacing(2),
		display: "flex",
		flexDirection: "row",
		justifyContent: "left",
		alignItems: "center",
		zIndex: 999
	}
});
