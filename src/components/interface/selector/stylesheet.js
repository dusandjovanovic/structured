export const styles = theme => ({
    container: {
        backgroundColor: theme.palette.background.paper,
        marginLeft: "1rem",
        marginRight: "1rem",
        minWidth: "90%",
        width: "auto",
        height: "auto"
    }
});